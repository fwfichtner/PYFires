# PyFires

## Installation

```bash
uv venv
source .venv/bin/activate
uv sync --no-dev
```

or with dev dependencies:

```bash
uv sync
```

Build the extension:

```bash
python setup.py build_ext --inplace
```
